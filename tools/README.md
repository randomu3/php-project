# AuraUI Code Tools

Централизованная система для автоматического форматирования и улучшения PHP кода.

## 🚀 Быстрый старт

### Запуск форматирования вручную
```cmd
tools\format-and-fix.cmd
```

### Автоматическое форматирование
Включите хук в `.kiro/hooks/format-on-save.json` - файлы будут форматироваться при сохранении.

## 📋 Основной скрипт

### `format-and-fix.cmd`
Запускает полный цикл форматирования и исправления кода:

1. **Исправление дубликатов PHPDoc** - удаляет повторяющиеся блоки документации
2. **Добавление недостающих PHPDoc** - добавляет @param и @return теги
3. **Форматирование PSR-12** - применяет стандарты кодирования PHP-CS-Fixer
4. **Синхронизация с хостом** - копирует исправленные файлы обратно

**Результат последнего запуска:**
- ✅ Обработано 21 файл
- ✅ Исправлен 1 файл (NotificationManager.php)
- ✅ Отформатировано 1 файл
- ⏱️ Время выполнения: ~2 секунды

## Отдельные скрипты

### `fix-phpdoc-duplicates.php`
Исправляет дублированные PHPDoc блоки и отступы.

**Что исправляет:**
- Удаляет повторяющиеся /** */ блоки перед методами/константами
- Исправляет отступы (4 пробела для констант и методов)
- Удаляет лишние пустые строки (более 2 подряд)

### `add-phpdoc-complete.php`
Добавляет недостающие PHPDoc теги.

**Что добавляет:**
- @return теги для методов без них
- Автоматически определяет описание по типу возврата:
  - `void` - без описания
  - `bool` - "True on success, false on failure"
  - `array` - "Array of data"
  - `int` - "Integer value"
  - `string` - "String value"
  - `self` - "Current instance"

## Структура директории

```
tools/
├── format-and-fix.cmd          # Главный скрипт
├── fix-phpdoc-duplicates.php   # Исправление дубликатов
├── add-phpdoc-complete.php     # Добавление PHPDoc
└── README.md                   # Эта документация
```

## Обрабатываемые директории

- `src/controllers/` - Контроллеры
- `src/helpers/` - Вспомогательные классы
- `src/core/` - Системные классы

## Интеграция с хуками

Эти инструменты используются в Kiro Hook для автоматического форматирования при сохранении файлов.

См. `.kiro/hooks/add-phpdoc-on-save.json`

## Требования

- Docker контейнер `phpproject-web-1` должен быть запущен
- PHP-CS-Fixer установлен через Composer
- Конфигурация `.php-cs-fixer.php` в корне проекта


## 🎯 Что исправляется

### 1. Дубликаты PHPDoc
**До:**
```php
/**
 * User password reset request constant
 *
 * @const
 */
/**
 * User password reset constant
 *
 * @const
 */
    /**
 * User password reset constant
 *
 * @const
 */
public const USER_PASSWORD_RESET_REQUEST = 'user.password_reset_request';
```

**После:**
```php
/**
 * User password reset request constant
 *
 * @const
 */
public const USER_PASSWORD_RESET_REQUEST = 'user.password_reset_request';
```

### 2. Отступы
**До:**
```php
public const USER_LOGIN = 'user.login';  // 0 пробелов
```

**После:**
```php
    public const USER_LOGIN = 'user.login';  // 4 пробела
```

### 3. Недостающие @return теги
**До:**
```php
/**
 * Get user notifications
 *
 * @param int $userId User ID
 */
public function getUserNotifications(int $userId): array
```

**После:**
```php
/**
 * Get user notifications
 *
 * @param int $userId User ID
 *
 * @return array Array of data
 */
public function getUserNotifications(int $userId): array
```

## 🔧 Интеграция с IDE

### Kiro Hook (Автоматический режим)
Файл: `.kiro/hooks/format-on-save.json`

**Включить:**
```json
{
  "enabled": true
}
```

**Отключить:**
```json
{
  "enabled": false
}
```

При включенном хуке все PHP файлы в `src/` будут автоматически форматироваться при сохранении.

## 📊 Статистика

### Обрабатываемые файлы
- Controllers: 6 файлов
- Helpers: 9 файлов
- Core: 6 файлов
- **Всего: 21 файл**

### Типичные проблемы
- ❌ Дублированные PHPDoc блоки
- ❌ Неправильные отступы (0 вместо 4 пробелов)
- ❌ Отсутствующие @return теги
- ❌ Более 2 пустых строк подряд

## 🛠️ Технические детали

### Используемые инструменты
- **PHP-CS-Fixer 3.90.0** - форматирование по PSR-12
- **Custom PHP scripts** - исправление PHPDoc
- **Docker** - изолированная среда выполнения

### Конфигурация PHP-CS-Fixer
Файл: `src/.php-cs-fixer.php`

Применяемые правила:
- PSR-12 стандарт
- Упорядочивание use statements
- Удаление неиспользуемых импортов
- Форматирование массивов
- Нормализация пробелов

## 📝 Примеры использования

### Разовое форматирование
```cmd
# Форматировать весь проект
tools\format-and-fix.cmd

# Только исправить дубликаты
docker exec phpproject-web-1 php /var/www/tools/fix-phpdoc-duplicates.php

# Только добавить PHPDoc
docker exec phpproject-web-1 php /var/www/tools/add-phpdoc-complete.php

# Только PHP-CS-Fixer
docker exec phpproject-web-1 vendor/bin/php-cs-fixer fix /var/www/html
```

### Форматирование конкретного файла
```cmd
docker exec phpproject-web-1 vendor/bin/php-cs-fixer fix /var/www/html/helpers/NotificationManager.php
```

## 🚨 Важные замечания

1. **Всегда делайте backup** перед массовым форматированием
2. **Проверяйте изменения** после автоматического форматирования
3. **Не редактируйте файлы** во время работы скрипта
4. **Docker должен быть запущен** для работы инструментов

## 🔄 Обновление инструментов

После изменения скриптов в `tools/`:

```cmd
# Скопировать в контейнер
docker cp tools phpproject-web-1:/var/www/

# Или отдельные файлы
docker cp tools/fix-phpdoc-duplicates.php phpproject-web-1:/var/www/tools/
docker cp tools/add-phpdoc-complete.php phpproject-web-1:/var/www/tools/
```

## 📚 Дополнительная документация

- [PHPDoc Standards](.kiro/steering/phpdoc.md)
- [Project Structure](.kiro/steering/structure.md)
- [Technology Stack](.kiro/steering/tech.md)
