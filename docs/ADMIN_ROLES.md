# 👤 Система ролей и прав администратора

## ✅ Что реализовано

Добавлена система ролей с правами администратора для доступа к админ-панели.

## 🔐 Права доступа

### Обычный пользователь
- ✅ Регистрация
- ✅ Авторизация
- ✅ Восстановление пароля
- ✅ Доступ к главной странице
- ❌ Доступ к админ-панели

### Администратор
- ✅ Все права обычного пользователя
- ✅ Доступ к админ-панели
- ✅ Просмотр базы данных
- ✅ Редактирование шаблонов писем
- ✅ Отправка писем

## 👑 Текущие администраторы

| Username | Email | Роль |
|----------|-------|------|
| demiz99 | demiz99@mail.ru | Администратор |

## 🎯 Как войти в админку

### Шаг 1: Войдите в систему
http://localhost:8080/login

**Учетные данные администратора:**
- Username: `demiz99`
- Password: `SecurePass123!`

### Шаг 2: Откройте админ-панель
http://localhost:8080/admin

### Быстрый вход
Запустите скрипт:
```powershell
.\quick-admin-login.ps1
```

## 🔧 Управление администраторами

### Сделать пользователя администратором

```bash
docker-compose -f docker-compose.dev.yml exec db mysql -uapp_user -papp_password app_db -e "UPDATE users SET is_admin = TRUE WHERE email = 'user@example.com';"
```

### Убрать права администратора

```bash
docker-compose -f docker-compose.dev.yml exec db mysql -uapp_user -papp_password app_db -e "UPDATE users SET is_admin = FALSE WHERE email = 'user@example.com';"
```

### Посмотреть всех администраторов

```bash
docker-compose -f docker-compose.dev.yml exec db mysql -uapp_user -papp_password app_db -e "SELECT username, email, is_admin FROM users WHERE is_admin = TRUE;"
```

## 📊 Структура БД

Добавлено поле `is_admin` в таблицу `users`:

```sql
ALTER TABLE users ADD COLUMN is_admin BOOLEAN DEFAULT FALSE;
```

## 🛡️ Безопасность

### Функции в config.php

```php
// Проверка авторизации
requireLogin()

// Проверка прав администратора
isAdmin()

// Требовать права администратора (или показать ошибку)
requireAdmin()
```

### Использование в коде

```php
<?php
require_once 'config.php';

// Для обычных страниц
requireLogin();

// Для админ-панели
requireAdmin();
?>
```

## 🚫 Сообщение об ошибке

Если пользователь без прав администратора попытается зайти в админку, он увидит:

```
🚫 Доступ запрещен

У вас нет прав администратора для доступа к этой странице.

[Вернуться на главную]
```

## 💡 Рекомендации

### Для разработки
- Используйте аккаунт `demiz99` для доступа к админке
- Все новые пользователи по умолчанию НЕ администраторы

### Для продакшна
1. Измените пароль администратора
2. Создайте отдельный аккаунт для каждого администратора
3. Регулярно проверяйте список администраторов
4. Логируйте действия администраторов

## 🎯 Примеры использования

### Создать нового администратора

1. Зарегистрируйте пользователя через /register
2. Сделайте его администратором:
```bash
docker-compose -f docker-compose.dev.yml exec db mysql -uapp_user -papp_password app_db -e "UPDATE users SET is_admin = TRUE WHERE username = 'newadmin';"
```

### Проверить права пользователя

```bash
docker-compose -f docker-compose.dev.yml exec db mysql -uapp_user -papp_password app_db -e "SELECT username, email, is_admin FROM users WHERE username = 'demiz99';"
```

## ✅ Готово!

Теперь только пользователи с правами администратора могут заходить в админ-панель.

Текущий администратор: **demiz99@mail.ru**
