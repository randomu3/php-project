# 🚀 Деплой на Production

## Обзор

Ваш проект использует Docker, что упрощает деплой. Есть два основных подхода:

1. **Docker на сервере** (рекомендуется) - переносите весь проект
2. **Традиционный хостинг** - только файлы PHP

---

## 📦 Подход 1: Docker на сервере (Рекомендуется)

### Преимущества
- ✅ Изолированная среда
- ✅ Одинаковое окружение везде
- ✅ Легко обновлять
- ✅ Автоматические миграции

### Требования к серверу
- Ubuntu 20.04+ / Debian 11+
- Docker & Docker Compose
- Минимум 1GB RAM
- 10GB свободного места

---

## 🔧 Первоначальная настройка Production

### Шаг 1: Подготовка сервера

```bash
# Подключитесь к серверу
ssh user@your-server.com

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установите Docker Compose
sudo apt-get update
sudo apt-get install docker-compose-plugin

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER
```

### Шаг 2: Клонирование проекта

```bash
# Создайте директорию для проекта
mkdir -p /var/www/myproject
cd /var/www/myproject

# Клонируйте репозиторий (или загрузите файлы)
git clone https://github.com/your-repo/project.git .

# Или загрузите через SCP/FTP
```

### Шаг 3: Настройка окружения

```bash
# Создайте .env файл для production
cat > .env << EOF
# Database
DB_HOST=db
DB_NAME=app_db
DB_USER=app_user
DB_PASS=STRONG_PASSWORD_HERE

# Email (Resend)
RESEND_API_KEY=your_production_api_key
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=Your App Name

# Security
SESSION_SECURE=1
EOF

# Установите правильные права
chmod 600 .env
```

### Шаг 4: Запуск в Production

```bash
# Соберите и запустите контейнеры
docker-compose up --build -d

# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs -f web
```

### Шаг 5: Применение миграций

```bash
# Миграции применяются автоматически при первом запуске
# Но можно запустить вручную:
docker-compose exec web php /var/www/html/database/migrate.php

# Или используйте скрипт (если на Windows сервере)
.\scripts\migrate.ps1
```

---

## 🔄 Обновление на Production (с сохранением данных)

### Сценарий: Новая версия сайта + старые пользователи

**Важно:** База данных хранится в Docker volume, она НЕ удаляется при обновлении!

### Шаг 1: Подготовка

```bash
# Создайте бэкап БД (ОБЯЗАТЕЛЬНО!)
docker-compose exec db mysqldump -u app_user -p app_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Или используйте скрипт
.\scripts\backup.ps1
```

### Шаг 2: Обновление кода

```bash
# Остановите контейнеры (БД не удаляется!)
docker-compose down

# Обновите код
git pull origin main
# Или загрузите новые файлы

# Пересоберите контейнеры
docker-compose up --build -d
```

### Шаг 3: Миграции применяются автоматически

**Как это работает:**

1. При запуске контейнера выполняется скрипт `database/migrate.php`
2. Скрипт проверяет какие миграции уже применены (таблица `migrations`)
3. Применяет только новые миграции
4. Старые данные остаются нетронутыми

**Пример:**

```
Было:
- 001_create_users.sql ✅ (уже применена)
- 002_create_sessions.sql ✅ (уже применена)

Добавили:
- 003_add_avatar_field.sql ❌ (новая)

Результат:
- Применится только 003_add_avatar_field.sql
- Все пользователи останутся в БД
- Добавится только новое поле avatar
```

### Шаг 4: Проверка

```bash
# Проверьте что все работает
docker-compose logs -f web

# Проверьте БД
docker-compose exec db mysql -u app_user -p app_db -e "SHOW TABLES;"

# Проверьте пользователей
docker-compose exec db mysql -u app_user -p app_db -e "SELECT COUNT(*) FROM users;"
```

---

## 📊 Работа с базой данных

### Структура хранения

```
Docker Volume: mysql_data
├── app_db/
│   ├── users (таблица)
│   ├── password_resets (таблица)
│   ├── email_templates (таблица)
│   ├── migrations (таблица - история миграций)
│   └── ... другие таблицы
```

### Где хранятся данные?

```bash
# Данные БД хранятся в Docker volume
docker volume ls
# Вывод: mysql_data

# Посмотреть где физически
docker volume inspect mysql_data
# Обычно: /var/lib/docker/volumes/mysql_data/_data
```

### Важно! 🔴

**Docker volume НЕ удаляется при:**
- `docker-compose down` - останавливает контейнеры
- `docker-compose up --build` - пересобирает контейнеры
- Обновлении кода

**Docker volume УДАЛЯЕТСЯ при:**
- `docker-compose down -v` - флаг `-v` удаляет volumes
- `docker volume rm mysql_data` - явное удаление

---

## 🔄 Миграции: Как это работает

### Файл миграции (пример)

```sql
-- database/migrations/003_add_avatar_field.sql

-- Добавляем поле avatar к таблице users
ALTER TABLE users ADD COLUMN avatar VARCHAR(255) NULL AFTER email;

-- Миграция идемпотентна (можно запускать несколько раз)
-- Если поле уже есть - ошибки не будет
```

### Скрипт миграции

```php
// database/migrate.php

// 1. Проверяет таблицу migrations
// 2. Читает все файлы из database/migrations/
// 3. Применяет только те, которых нет в таблице migrations
// 4. Записывает примененные миграции

// Таблица migrations:
// id | migration_name              | applied_at
// 1  | 001_create_users.sql        | 2025-11-20 10:00:00
// 2  | 002_create_sessions.sql     | 2025-11-20 10:00:01
// 3  | 003_add_avatar_field.sql    | 2025-11-23 15:30:00
```

### Создание новой миграции

```bash
# 1. Создайте файл с номером
# database/migrations/004_add_notifications.sql

# 2. Напишите SQL
CREATE TABLE IF NOT EXISTS notifications (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

# 3. При следующем деплое миграция применится автоматически
```

---

## 🎯 Сценарии обновления

### Сценарий 1: Только код (без изменений БД)

```bash
# 1. Бэкап (на всякий случай)
docker-compose exec db mysqldump -u app_user -p app_db > backup.sql

# 2. Обновление
docker-compose down
git pull
docker-compose up --build -d

# 3. Готово! Пользователи остались, код обновлен
```

### Сценарий 2: Код + новые миграции

```bash
# 1. Бэкап
docker-compose exec db mysqldump -u app_user -p app_db > backup.sql

# 2. Обновление кода (с новыми миграциями)
docker-compose down
git pull
docker-compose up --build -d

# 3. Миграции применятся автоматически
# 4. Проверьте логи
docker-compose logs web | grep "Migration"
```

### Сценарий 3: Откат к предыдущей версии

```bash
# 1. Остановите контейнеры
docker-compose down

# 2. Откатите код
git checkout previous-version

# 3. Восстановите БД из бэкапа (если нужно)
docker-compose up -d db
docker-compose exec -T db mysql -u app_user -p app_db < backup.sql

# 4. Запустите
docker-compose up -d
```

---

## 🔒 Безопасность Production

### 1. Измените пароли

```bash
# В .env файле:
DB_PASS=STRONG_RANDOM_PASSWORD_HERE
RESEND_API_KEY=your_production_key
```

### 2. Включите HTTPS

```bash
# Используйте Nginx + Let's Encrypt
# Или Cloudflare (бесплатно)
```

### 3. Настройте firewall

```bash
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 80/tcp   # HTTP
sudo ufw allow 443/tcp  # HTTPS
sudo ufw enable
```

### 4. Регулярные бэкапы

```bash
# Создайте cron job для автоматических бэкапов
crontab -e

# Добавьте:
0 2 * * * cd /var/www/myproject && docker-compose exec -T db mysqldump -u app_user -pPASSWORD app_db > /backups/db_$(date +\%Y\%m\%d).sql
```

---

## 📈 Мониторинг

### Проверка здоровья

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f web
docker-compose logs -f db

# Использование ресурсов
docker stats

# Проверка БД
docker-compose exec db mysql -u app_user -p app_db -e "SELECT COUNT(*) FROM users;"
```

### Метрики

```bash
# Размер БД
docker-compose exec db mysql -u app_user -p app_db -e "
SELECT 
    table_schema AS 'Database',
    ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) AS 'Size (MB)'
FROM information_schema.tables
WHERE table_schema = 'app_db'
GROUP BY table_schema;
"
```

---

## 🆘 Troubleshooting

### Проблема: Миграции не применяются

```bash
# Проверьте таблицу migrations
docker-compose exec db mysql -u app_user -p app_db -e "SELECT * FROM migrations;"

# Примените вручную
docker-compose exec web php /var/www/html/database/migrate.php
```

### Проблема: Потеряны данные

```bash
# Восстановите из бэкапа
docker-compose up -d db
docker-compose exec -T db mysql -u app_user -p app_db < backup.sql
```

### Проблема: Контейнер не запускается

```bash
# Проверьте логи
docker-compose logs web
docker-compose logs db

# Пересоберите
docker-compose down
docker-compose up --build -d
```

---

## 📝 Чеклист деплоя

### Перед деплоем:
- [ ] Создан бэкап БД
- [ ] Протестировано локально
- [ ] Обновлены переменные окружения (.env)
- [ ] Проверены миграции
- [ ] Обновлен ASSET_VERSION в config.php

### Во время деплоя:
- [ ] `docker-compose down`
- [ ] Обновление кода (`git pull`)
- [ ] `docker-compose up --build -d`
- [ ] Проверка логов
- [ ] Проверка сайта

### После деплоя:
- [ ] Проверка всех страниц
- [ ] Проверка авторизации
- [ ] Проверка email
- [ ] Проверка админ-панели
- [ ] Создание нового бэкапа

---

## 🎓 Резюме

### Как работает обновление:

1. **Код обновляется** → новые файлы PHP
2. **БД остается** → все пользователи сохраняются
3. **Миграции применяются** → только новые изменения
4. **Старые данные нетронуты** → ничего не удаляется

### Ключевые моменты:

- ✅ Docker volume хранит БД отдельно от кода
- ✅ Миграции применяются автоматически
- ✅ Старые пользователи остаются
- ✅ Можно откатиться к предыдущей версии
- ✅ Бэкапы - ваша страховка

### Команды для запоминания:

```bash
# Бэкап
docker-compose exec db mysqldump -u app_user -p app_db > backup.sql

# Обновление
docker-compose down && git pull && docker-compose up --build -d

# Проверка
docker-compose logs -f web
```

---

## 📚 Дополнительные ресурсы

- [Docker Documentation](https://docs.docker.com/)
- [MySQL Backup Best Practices](https://dev.mysql.com/doc/refman/8.0/en/backup-and-recovery.html)
- [PHP Deployment Guide](https://www.php.net/manual/en/install.php)
