# Настройка Email через Resend

## ✅ Email работает!

Письмо успешно отправлено на `demiz99@mail.ru` с ID: `caffbc95-563c-4a87-8e58-2179a6f8526d`

## 🧪 Тестирование

Откройте в браузере: http://localhost:8080/test_email

Эта страница позволяет:
- Отправить тестовое письмо
- Проверить настройки Resend
- Увидеть текущую конфигурацию

## ⚠️ Важно: Ограничения тестового режима

В тестовом режиме (без верифицированного домена) Resend отправляет письма **только на email владельца аккаунта**: `demiz99@mail.ru`

### Что это значит:
- ✅ Письма на `demiz99@mail.ru` будут доставлены
- ❌ Письма на другие адреса (test@example.com и т.д.) НЕ будут доставлены
- ⚠️ Вы увидите ошибку: "You can only send testing emails to your own email address"

## 🚀 Как отправлять на любые адреса

### Вариант 1: Верифицировать домен (рекомендуется)

1. Зайдите на https://resend.com/domains
2. Добавьте свой домен (например, `mysite.com`)
3. Настройте DNS записи (SPF, DKIM, DMARC)
4. После верификации измените `FROM_EMAIL` в `docker-compose.dev.yml`:
   ```yaml
   FROM_EMAIL: noreply@mysite.com
   ```

### Вариант 2: Использовать другой сервис

Если нет домена, можно использовать:
- **Mailgun** (бесплатно 5000 писем/месяц)
- **SendGrid** (бесплатно 100 писем/день)
- **SMTP Gmail** (для разработки)

## 📧 Текущие настройки

```env
RESEND_API_KEY=re_brMPxT9m_BEgFoPQucTe22E1QcAw5svTH
FROM_EMAIL=onboarding@resend.dev
FROM_NAME=Мой сайт
```

## 🔍 Проверка логов

Смотрите логи отправки:
```bash
docker-compose -f docker-compose.dev.yml logs web | grep "Email"
```

Успешная отправка:
```
✅ Email sent successfully to: demiz99@mail.ru, Subject: Добро пожаловать!, ID: caffbc95-...
```

Ошибка:
```
❌ Email error to test@example.com: You can only send testing emails to your own email address
```

## 🎯 Что работает сейчас

1. **Регистрация** - отправляет приветственное письмо на `demiz99@mail.ru`
2. **Восстановление пароля** - отправляет ссылку на `demiz99@mail.ru`
3. **Тестовая страница** - `/test_email` для проверки

## 💡 Рекомендации

Для продакшна:
1. Верифицируйте домен на Resend
2. Используйте переменные окружения для API ключа
3. Настройте мониторинг доставки писем
4. Добавьте retry логику для failed emails
