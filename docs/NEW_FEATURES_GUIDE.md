# Руководство по новым функциям

## 🎉 Что добавлено

### 1. Система ролей и прав доступа 👑
### 2. Логирование действий 📝
### 3. Уведомления в системе 🔔

---

## 1️⃣ Система ролей и прав доступа

### Описание
Гибкая система управления ролями и правами пользователей.

### Базовые роли:
- **admin** - Администратор (полный доступ)
- **moderator** - Модератор (управление контентом и пользователями)
- **user** - Пользователь (базовый доступ)

### Категории прав:
- **users** - Управление пользователями
- **roles** - Управление ролями
- **settings** - Управление настройками
- **emails** - Управление email
- **logs** - Просмотр логов
- **notifications** - Управление уведомлениями

### Использование в коде:

```php
// Проверить роль
if (hasRole('admin')) {
    // Код для админа
}

// Проверить право
if (hasPermission('users.edit')) {
    // Код для редактирования пользователей
}

// Требовать право (или 403 ошибка)
requirePermission('users.delete');

// Работа с RoleManager
$rm = new RoleManager();

// Назначить роль
$rm->assignRole($userId, $roleId);

// Удалить роль
$rm->removeRole($userId, $roleId);

// Получить роли пользователя
$roles = $rm->getUserRoles($userId);

// Получить права пользователя
$permissions = $rm->getUserPermissions($userId);
```

### Таблицы БД:
- `roles` - Роли
- `permissions` - Права доступа
- `role_permissions` - Связь ролей и прав
- `user_roles` - Связь пользователей и ролей

---

## 2️⃣ Логирование действий

### Описание
Автоматическое логирование всех важных действий пользователей.

### Что логируется:
- ✅ Вход/выход из системы
- ✅ Регистрация
- ✅ Изменение профиля
- ✅ Смена пароля
- ✅ Действия администратора
- ✅ Отправка email
- ✅ Изменение настроек

### Использование в коде:

```php
// Простое логирование
logActivity('user.action', 'Описание действия');

// С указанием сущности
logActivity(
    'user.update', 
    'Обновлен профиль пользователя', 
    'user',  // тип сущности
    $userId  // ID сущности
);

// Использование предопределенных действий
logActivity(
    ActivityActions::USER_LOGIN, 
    "Пользователь {$username} вошел в систему"
);

// Работа с ActivityLogger
$logger = new ActivityLogger();

// Получить логи пользователя
$logs = $logger->getUserLogs($userId, 50);

// Получить все логи с фильтрами
$logs = $logger->getAllLogs(100, 0, [
    'user_id' => $userId,
    'action' => 'user.login',
    'date_from' => '2024-01-01',
    'date_to' => '2024-12-31'
]);

// Получить статистику
$stats = $logger->getStats(7); // за последние 7 дней

// Очистить старые логи
$logger->cleanOldLogs(90); // старше 90 дней
```

### Предопределенные действия:
```php
ActivityActions::USER_LOGIN
ActivityActions::USER_LOGOUT
ActivityActions::USER_REGISTER
ActivityActions::USER_UPDATE_PROFILE
ActivityActions::USER_CHANGE_PASSWORD
ActivityActions::ADMIN_USER_CREATE
ActivityActions::ADMIN_USER_EDIT
ActivityActions::EMAIL_SEND
ActivityActions::SETTINGS_UPDATE
ActivityActions::ROLE_ASSIGN
```

### Таблица БД:
- `activity_logs` - Логи действий

### Поля лога:
- `user_id` - ID пользователя
- `action` - Действие
- `description` - Описание
- `entity_type` - Тип сущности
- `entity_id` - ID сущности
- `ip_address` - IP адрес
- `user_agent` - User Agent
- `created_at` - Дата и время

---

## 3️⃣ Уведомления в системе

### Описание
Система внутренних уведомлений с колокольчиком в навигации.

### Типы уведомлений:
- **info** - Информационное (синий)
- **success** - Успешное действие (зеленый)
- **warning** - Предупреждение (желтый)
- **error** - Ошибка (красный)
- **system** - Системное (серый)

### Использование в коде:

```php
// Простое уведомление
notify(
    $userId, 
    NotificationTypes::SUCCESS, 
    'Заголовок', 
    'Сообщение'
);

// С ссылкой и иконкой
notify(
    $userId,
    NotificationTypes::WARNING,
    'Пароль изменен',
    'Ваш пароль был успешно изменен',
    '/profile',  // ссылка
    NotificationIcons::WARNING  // иконка
);

// Работа с NotificationManager
$nm = new NotificationManager();

// Создать уведомление
$nm->create($userId, 'info', 'Заголовок', 'Сообщение', '/link', 'icon');

// Получить уведомления
$notifications = $nm->getUserNotifications($userId, 10);

// Получить только непрочитанные
$unread = $nm->getUserNotifications($userId, 10, true);

// Количество непрочитанных
$count = $nm->getUnreadCount($userId);

// Отметить как прочитанное
$nm->markAsRead($notificationId, $userId);

// Отметить все как прочитанные
$nm->markAllAsRead($userId);

// Удалить уведомление
$nm->delete($notificationId, $userId);

// Уведомить всех пользователей
$nm->notifyAll('info', 'Заголовок', 'Сообщение');

// Уведомить пользователей с ролью
$nm->notifyRole('admin', 'info', 'Заголовок', 'Сообщение');
```

### Иконки:
```php
NotificationIcons::INFO          // info
NotificationIcons::SUCCESS       // check-circle
NotificationIcons::WARNING       // alert-triangle
NotificationIcons::ERROR         // alert-circle
NotificationIcons::SYSTEM        // settings
NotificationIcons::USER          // user
NotificationIcons::EMAIL         // mail
NotificationIcons::BELL          // bell
```

### UI компоненты:
- **Колокольчик в навигации** - Быстрый доступ к последним 5 уведомлениям
- **Страница /notifications** - Все уведомления пользователя
- **Счетчик непрочитанных** - Красный badge на колокольчике

### Таблица БД:
- `notifications` - Уведомления

### Поля уведомления:
- `user_id` - ID пользователя
- `type` - Тип (info, success, warning, error, system)
- `title` - Заголовок
- `message` - Сообщение
- `link` - Ссылка (опционально)
- `icon` - Иконка (опционально)
- `is_read` - Прочитано
- `created_at` - Дата создания
- `read_at` - Дата прочтения

---

## 🚀 Установка

### 1. Запустить миграции:

```powershell
.\scripts\run-new-migrations.ps1
```

### 2. Проверить работу:

```powershell
# Открыть приложение
start http://localhost:8080

# Войти в систему
# Проверить колокольчик уведомлений в навигации
# Перейти на /notifications
```

---

## 📝 Примеры использования

### Пример 1: Отправка уведомления при регистрации

```php
// В RegisterController.php
notify(
    $userId,
    NotificationTypes::SUCCESS,
    'Добро пожаловать!',
    'Ваш аккаунт успешно создан. Спасибо за регистрацию!',
    '/profile',
    NotificationIcons::SUCCESS
);
```

### Пример 2: Логирование действия админа

```php
// При редактировании пользователя
logActivity(
    ActivityActions::ADMIN_USER_EDIT,
    "Администратор изменил данные пользователя {$username}",
    'user',
    $userId
);
```

### Пример 3: Проверка прав доступа

```php
// В контроллере
class UsersController {
    public function delete($userId) {
        // Требуем право на удаление
        requirePermission('users.delete');
        
        // Удаляем пользователя
        // ...
        
        // Логируем
        logActivity(
            ActivityActions::ADMIN_USER_DELETE,
            "Удален пользователь ID: {$userId}",
            'user',
            $userId
        );
        
        // Уведомляем админов
        $nm = new NotificationManager();
        $nm->notifyRole(
            'admin',
            NotificationTypes::WARNING,
            'Пользователь удален',
            "Пользователь ID {$userId} был удален из системы"
        );
    }
}
```

---

## 🔧 Настройка

### Автоматическая очистка старых данных

Добавьте в cron (или Task Scheduler):

```php
// cleanup.php
require_once 'config.php';
require_once 'helpers/ActivityLogger.php';
require_once 'helpers/NotificationManager.php';

$logger = new ActivityLogger();
$logger->cleanOldLogs(90); // Удалить логи старше 90 дней

$nm = new NotificationManager();
$nm->cleanOldNotifications(30); // Удалить прочитанные уведомления старше 30 дней
```

---

## 📊 Мониторинг

### Просмотр логов в админке

Создайте страницу для просмотра логов:

```php
// В AdminController
requirePermission('logs.view');

$logger = new ActivityLogger();
$logs = $logger->getAllLogs(100);

// Отобразить в таблице
```

### Статистика уведомлений

```php
$nm = new NotificationManager();

// Количество непрочитанных у всех пользователей
$stmt = $db->query("
    SELECT COUNT(*) as count 
    FROM notifications 
    WHERE is_read = 0
");
```

---

## ✅ Готово!

Все три системы полностью интегрированы и готовы к использованию:

1. ✅ **Роли и права** - Гибкое управление доступом
2. ✅ **Логирование** - Аудит всех действий
3. ✅ **Уведомления** - Информирование пользователей

**Следующие шаги:**
- Добавить UI для управления ролями в админке
- Добавить страницу просмотра логов
- Настроить автоматическую очистку старых данных
