# 🔧 Исправление доступа к админке

## Проблема
Не можете зайти в админку даже под demiz99

## ✅ Решение

### Шаг 1: Выйдите из системы
http://localhost:8080/logout

### Шаг 2: Войдите заново
http://localhost:8080/login
- Username: `demiz99`
- Password: `SecurePass123!`

### Шаг 3: Проверьте права
http://localhost:8080/check_admin

Должно показать:
- ✅ У вас ЕСТЬ права администратора!

### Шаг 4: Откройте админку
http://localhost:8080/admin

## 🐛 Если всё ещё не работает

### Проверка 1: Пользователь в БД
```bash
docker-compose -f docker-compose.dev.yml exec db mysql -uapp_user -papp_password app_db -e "SELECT id, username, email, is_admin FROM users WHERE username = 'demiz99';"
```

Должно быть: `is_admin = 1`

### Проверка 2: Очистите сессии
```bash
docker-compose -f docker-compose.dev.yml restart web
```

Затем войдите заново.

### Проверка 3: Проверьте логи
```bash
docker-compose -f docker-compose.dev.yml logs web --tail 20
```

## 🔍 Отладка

Откройте: http://localhost:8080/check_admin

Эта страница покажет:
- Авторизованы ли вы
- Ваш User ID
- Данные из БД
- Значение is_admin
- Результат функции isAdmin()

## 💡 Быстрое решение

Если ничего не помогает:

1. Остановите контейнеры:
```bash
docker-compose -f docker-compose.dev.yml down
```

2. Запустите заново:
```bash
docker-compose -f docker-compose.dev.yml up -d
```

3. Подождите 5 секунд

4. Войдите: http://localhost:8080/login

5. Админка: http://localhost:8080/admin

## ✅ Должно работать!

Если после всех шагов всё ещё не работает - сообщите что показывает `/check_admin`
