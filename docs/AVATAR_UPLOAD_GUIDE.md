# Руководство по загрузке аватара

## ✅ Что реализовано

### Функциональность:
- ✅ Загрузка аватара (JPG, PNG, GIF, WebP)
- ✅ Автоматическое изменение размера до 300x300px
- ✅ Удаление старого аватара при загрузке нового
- ✅ Удаление аватара
- ✅ Отображение аватара в профиле
- ✅ Отображение аватара в навигации
- ✅ Дефолтный аватар с первой буквой имени
- ✅ Логирование действий
- ✅ Уведомления

### Ограничения:
- Максимальный размер: 5MB
- Форматы: JPG, JPEG, PNG, GIF, WebP
- Автоматическое изменение размера: 300x300px

## 📁 Созданные файлы

```
database/migrations/009_add_avatar_field.sql
src/helpers/ImageUploader.php
src/uploads/avatars/.htaccess
src/uploads/avatars/.gitkeep
```

## 🔧 Обновленные файлы

```
src/controllers/ProfileController.php
src/views/partials/profile_info_tab.php
src/index.php
```

## 🚀 Использование

### Для пользователя:

1. **Загрузить аватар:**
   - Перейти в профиль
   - Навести на аватар
   - Нажать на иконку камеры
   - Выбрать изображение
   - Аватар загрузится автоматически

2. **Удалить аватар:**
   - Перейти в профиль
   - Нажать "Удалить аватар" под аватаром
   - Подтвердить удаление

### Для разработчика:

```php
// Загрузить аватар
$uploader = new ImageUploader();
$result = $uploader->uploadAvatar($_FILES['avatar'], $userId);

if ($result['success']) {
    $filename = $result['filename'];
    $path = $result['path'];
}

// Удалить аватар
$uploader->deleteAvatar($filename);

// Получить URL аватара
$avatarUrl = ImageUploader::getAvatarUrl($user['avatar'], $user['username']);
```

## 🎨 Где отображается аватар

1. **Профиль** (`/profile`) - большой аватар 128x128px
2. **Навигация** (`index.php`) - маленький аватар 32x32px
3. **Админ-панель** (будущее) - в списке пользователей

## 🔒 Безопасность

- ✅ Проверка типа файла (MIME type)
- ✅ Проверка расширения
- ✅ Проверка размера (макс 5MB)
- ✅ Уникальные имена файлов
- ✅ Защита папки uploads через .htaccess
- ✅ CSRF защита
- ✅ Автоматическое изменение размера

## 📊 Технические детали

### Формат имени файла:
```
avatar_{userId}_{timestamp}.{extension}
```

Пример: `avatar_1_1700000000.jpg`

### Путь к файлу:
```
src/uploads/avatars/avatar_1_1700000000.jpg
```

### URL:
```
/uploads/avatars/avatar_1_1700000000.jpg
```

### Поле в БД:
```sql
avatar VARCHAR(255) NULL
```

Хранится только имя файла: `avatar_1_1700000000.jpg`

## 🎯 Логирование

При загрузке аватара:
```
Action: user.update_profile
Description: Загружен новый аватар
```

При удалении аватара:
```
Action: user.update_profile
Description: Удален аватар
```

## 🔔 Уведомления

При загрузке:
```
Type: success
Title: Аватар обновлен
Message: Ваш аватар успешно загружен
```

## 🐛 Troubleshooting

### Ошибка "Файл слишком большой"
- Проверьте размер файла (макс 5MB)
- Проверьте настройки PHP: `upload_max_filesize` и `post_max_size`

### Ошибка "Не удалось сохранить файл"
- Проверьте права на папку `src/uploads/avatars/` (должно быть 755)
- Проверьте, что папка существует

### Аватар не отображается
- Проверьте, что файл существует в `src/uploads/avatars/`
- Проверьте путь в БД
- Очистите кэш браузера (Ctrl+F5)

### Ошибка GD extension
- Убедитесь, что расширение GD установлено в PHP
- Проверьте: `php -m | grep gd`

## ✅ Готово!

Функция загрузки аватара полностью реализована и готова к использованию.

**Протестируйте:**
1. Откройте http://localhost:8080/profile
2. Наведите на аватар
3. Нажмите на иконку камеры
4. Выберите изображение
5. Аватар загрузится автоматически
6. Проверьте отображение в навигации
